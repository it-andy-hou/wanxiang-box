# 需求记录

## 2025年11月19日 
1、导入完成后，导入弹窗界面提示导入成功后就可以自动关掉了。
2、导入完成后，主机管理页面自动刷新一下，不用手动刷新页面。
3、主机管理，筛选完系统，应用筛选、机房、环境筛选就应该只有对应系统的，不用仍然是全量筛选
4、执行任务，选择目标主机的时候，能显示出来勾选的主机数量 
5、执行任务，选择目标主机的时候，遵循主机管理页面的筛选逻辑，筛选完系统，应用筛选、机房、环境筛选就应该只有对应系统的，不用是全量筛选
6、批量导入主机可以不使用csv格式吗？如果使用excel 格式是不是行，目前csv格式必须要UTF-8的编码，换成excel格式是否可以不用在意编码问题？
7、导入、导出csv按钮 改成 导入、导出 excel
8、Excel批量导入主机弹窗页面 提供一个下载导入excel的模板文件，方便用户直接按要求导入
下载的模板文件表头
IP地址,端口,用户名,密码,描述,环境,系统名称,应用名称,机房
127.0.0.1,22,root,123456,测试,灰度,统一接入子系统,应用1,机房1
9、修改 Excel批量导入主机 提示 为
Excel文件：
• 第一行为表头：IP地址,端口,用户名,密码,描述,环境,系统名称,应用名称,机房
• 💡 首次导入？点击「下载模板」获取标准格式文件


## 2025年11月22日
1、任务执行完成后，下方显示实时日志的地方，下载日志按钮旁边 新增一个 复制日志 的按钮，点击可以直接复制日志内容到粘贴板中。
2、任务执行，选择目标主机，目前是通过筛选勾选完成的，新增一个输入识别框，可以输入一组IP地址用逗号分隔，就会自动选中管理的主机，如果输入中的主机列表中的IP地址不存在于管理的主机中，会提示输入的具体IP地址不存在于管理的主机中。

## 2025年11月24日
1、新增一个脚本管理页面，可以批量导入导出脚本，支持Excel格式。
2、修复脚本页面的滚动条
3、脚本管理页面，新增一个标签筛选的功能
4、导入导出脚本按钮移动到 刷新列表的右边
5、任务执行改名为脚本执行
6、主机管理页面，批量选中主机后，新增一个复制按钮，可以复制IP列表到粘贴板，格式是IP以逗号分隔。
7、新增一个命令执行页面，功能和脚本执行页面功能一致，

实施方案确认
1. 页面命名
导航菜单：命令执行
页面ID：command-execution
2. 命令输入形式
复用脚本管理中的多行文本框样式
支持多行编辑
与"添加脚本"中的脚本内容输入框保持一致的UI
3. 参数支持
不支持参数模板（${IP}, ${HOSTNAME} 等）
直接执行原始命令内容
4. 历史记录标识
在执行历史中明确区分"临时命令"和"保存的脚本"
可能需要：
在 task_history 表中增加字段标识类型（如 is_temporary_command）
或通过 script_id 为 null 来判断
5. 实现范围
需要修改/新增的文件：
导航菜单：src/renderer/js/navigation.js - 添加"命令执行"入口
主页面：src/renderer/index.html - 添加命令执行页面区块
命令执行逻辑：src/renderer/js/commandExecution.js（新建） - 参考 taskExecution.js
历史记录：src/renderer/js/taskHistory.js - 增加临时命令标识显示
数据库模型：可能需要调整 task_history 表结构（如果现有字段不够用）
复用的逻辑：
主机选择（步骤2）
任务执行器（src/main/taskExecutor.js）
SSH服务（src/main/sshService.js）
��行进度监控

## 2025年11月25日
1、新增一个文件上传页面，功能和命令执行页面功能基本一致。

### 功能需求

#### 相同点（继承自命令执行功能）
- **主机选择器**：支持筛选（区域/环境/业务/主机类型）、IP批量输入、搜索、全选等
- **执行流程**：批量并发执行、实时进度展示、日志输出
- **历史记录**：执行记录保存和查看

#### 核心差异点
1. **命令编辑区域替换为文件上传配置区域**
   - 原来：多行命令编辑器（支持语法高亮）
   - 现在：文件选择 + 目标路径配置

2. **文件选择功能**
   - 新增"选择文件"按钮
   - 支持多文件选择（不支持目录上传）
   - 显示已选文件列表和数量
   - 点击后弹出本地文件选择对话框

3. **目标路径配置**
   - 填写目标主机上的存放目录（如 `/opt/app/`）
   - 自动拼接显示完整路径预览（如 `/opt/app/config.ini`）

4. **配置选项A：文件冲突处理策略**
   - ○ 覆盖（直接替换已存在文件）
   - ○ 跳过（保留原文件，不上传）
   - ● 备份后覆盖（默认，重命名为 `.bak.时间戳` 后再上传）

5. **配置选项B：目标路径不存在时**
   - ○ 自动创建（执行 `mkdir -p` 创建目录）
   - ● 报错中止（默认，不创建，任务失败）

#### 执行逻辑
- 通过 SCP/SFTP 上传到每个勾选的目标主机
- 显示上传进度（百分比进度条）
- 上传完成后显示每台主机的上传结果（成功/失败）

#### 实时日志示例
```
[192.168.1.10] 开始上传到 /opt/app/
[192.168.1.10] 检查目录...目录不存在，报错中止
[192.168.1.10] ✗ 上传失败：目标路径不存在

[192.168.1.11] 开始上传到 /opt/app/
[192.168.1.11] 检查目录...目录已存在
[192.168.1.11] 上传 config.ini (45%) ████████░░░░░░░░
[192.168.1.11] ✓ config.ini 上传成功 (检测到已存在，已备份为 config.ini.bak.20251125_143022)
[192.168.1.11] 上传 app.jar (78%) ██████████████░░
[192.168.1.11] ✓ app.jar 上传成功
[192.168.1.11] ✓ 完成 (2/2 文件成功)
```

#### 历史记录存储
- 记录类型标识：`is_file_upload = 1`
- 存储字段：
  - 源文件列表（JSON格式存储多个文件名）
  - 目标路径
  - 文件冲突策略（覆盖/跳过/备份后覆盖）
  - 路径创建策略（自动创建/报错中止）
  - 执行结果（成功/失败主机列表）

#### 界面命名
- 页面名称：**文件上传**
- 导航菜单显示名称：**文件上传**
- 页面ID：`file-upload`

### 实施方案

#### 需要新增/修改的文件清单
1. **前端页面**：`src/renderer/js/fileUpload.js`（新建，参考 `commandExecution.js`）
2. **导航菜单**：`src/renderer/index.html` 增加"文件上传"菜单项
3. **数据库表**：`src/database/init.js` 和 `models.js` 扩展 `task_executions` 表
   - 新增字段：`is_file_upload`（标识文件上传任务）
   - 新增字段：`upload_files`（JSON存储文件列表）
   - 新增字段：`target_path`（目标路径）
   - 新增字段：`conflict_strategy`（文件冲突策略）
   - 新增字段：`path_create_strategy`（路径创建策略）
4. **主进程执行器**：`src/main/taskExecutor.js` 增加文件上传逻辑（调用 SCP）
5. **SSH服务**：`src/main/sshService.js` 增加文件上传方法

### 复用的逻辑
- 主机选择器（完全复用命令执行页面的实现）
- 任务执行器框架（`src/main/taskExecutor.js`）
- SSH连接管理（`src/main/sshService.js`）
- 执行进度监控与日志展示
- 历史记录查询与展示

---

## 2025年11月25日 - 定时任务功能

### 需求背景
当前"脚本执行"和"命令执行"功能创建任务后点击"开始执行"会立即执行，缺乏定时执行能力。需要新增定时执行功能和任务管理页面。

### 功能需求

#### 1. 定时执行选项（采用方案1：默认立即 + 可选定时）

**界面设计原则**：
- ✅ 零额外操作成本：默认立即执行，无需任何勾选
- ✅ 简单可选：需要定时时勾选一个复选框即可
- ✅ 界面简洁：不占用额外空间，体验流畅

**在创建任务时（脚本执行 & 命令执行页面）界面布局**：
```
步骤 1: 选择执行脚本 *
[下拉框]

步骤 2: 选择目标主机 *
[从主机管理选择] [清空选择]

□ 定时执行  💡                     ← 默认不勾选
  └→ 执行时间: [日期选择器]         ← 勾选后才显示

[开始执行] [取消]
```

**交互逻辑**：
- **默认行为**（不勾选"定时执行"）：点击"开始执行" → 立即执行任务（当前行为保持不变）
- **定时模式**（勾选"定时执行"）：
  - 显示日期时间选择器（HTML5 datetime-local）
  - 设置执行时间（不允许选择过去时间）
  - 点击"开始执行" → 任务保存为 `scheduled` 状态，不立即执行
  - 提示用户"定时任务已创建，将在 YYYY-MM-DD HH:mm:ss 自动执行"
  - 自动跳转到"任务调度"页面

#### 2. 任务管理页面（新页面）

**页面名称**：任务调度 / 定时任务管理

**显示内容**：所有**未开始**的定时任务列表

**任务状态筛选**：
- 待执行（scheduled）
- 全部定时任务

**表格列**：
| 任务ID | 任务名称 | 任务类型 | 目标主机数 | 计划执行时间 | 剩余时间 | 创建时间 | 操作 |

**支持的操作**：
- ✏️ **编辑任务**：修改任务配置（主机列表、脚本/命令内容、并发数、超时时间等）
- 🕒 **调整时间**：修改计划执行时间
- ▶️ **立即执行**：手动触发，跳过定时，直接执行任务
- 🗑️ **删除任务**：取消定时任务并删除

#### 3. 自动执行机制

**定时检查器**（主进程）：
- 每分钟检查一次数据库中状态为 `scheduled` 的任务
- 判断 `scheduled_time <= 当前时间`
- 满足条件则自动触发执行
- 执行完成后，任务状态更新为 `completed` / `failed`

**执行流程**：
```
 scheduled（待执行）
     ↓ (到达设定时间)
  running（执行中）
     ↓
 completed / failed（完成/失败）
```

### 技术实现要点

#### 数据库扩展（`task_executions` 表）
新增字段：
```sql
execution_type TEXT DEFAULT 'immediate',  -- 执行类型：immediate(立即)/scheduled(定时)
scheduled_time TEXT,                      -- 计划执行时间（ISO 8601格式）
```

任务状态（`status` 字段）扩展：
- `scheduled`：已调度（等待执行时间到达）
- `pending`：待执行（立即执行任务的初始状态）
- `running`：执行中
- `completed`：已完成
- `failed`：失败
- `cancelled`：已取消

#### 主进程服务
**新增文件**：`src/main/taskScheduler.js`
- 启动定时检查器（每分钟触发）
- 查询 `status = 'scheduled' AND scheduled_time <= NOW()` 的任务
- 调用 `taskExecutor.executeTask()` 执行任务
- 更新任务状态

#### 前端页面
**新增页面**：任务调度管理
- 位置：在"执行历史"前增加导航项
- 文件：`src/renderer/js/scheduledTasks.js`（新建）
- 界面：复用现有表格和弹窗组件

**修改现有页面**：
- `taskExecution.js`：增加执行类型选择和时间选择器
- `commandExecution.js`：增加执行类型选择和时间选择器
- `taskHistory.js`：区分显示立即任务和定时任务

#### 交互流程示例

**创建定时任务**：
1. 用户在"脚本执行"页面配置任务
2. 选择"定时执行"，设置时间为"2025-11-26 14:00:00"
3. 点击"开始执行"→ 任务保存为 `scheduled` 状态
4. 提示"任务已创建，将在 2025-11-26 14:00:00 自动执行"
5. 跳转到"任务调度"页面，显示该任务

**任务管理操作**：
1. 在"任务调度"页面查看待执行任务
2. 点击"调整时间"→ 弹窗修改执行时间
3. 点击"立即执行"→ 确认后立即执行，状态变为 `running`
4. 点击"删除"→ 确认后删除任务记录

**自动执行**：
1. 主进程定时器每分钟检查一次
2. 发现任务A的 `scheduled_time` 已到达
3. 自动调用执行器执行任务
4. 渲染进程如果在"任务调度"页面，实时刷新列表
5. 执行完成后，任务从"任务调度"列表消失，进入"执行历史"

### 涉及的文件清单

**新增文件**：
- `src/main/taskScheduler.js`：定时任务调度器
- `src/renderer/js/scheduledTasks.js`：任务调度管理页面逻辑

**修改文件**：
- `src/database/init.js`：扩展 `task_executions` 表字段
- `src/database/models.js`：更新 `TaskModel`，支持新字段
- `src/renderer/index.html`：新增"任务调度"导航和页面区块
- `src/renderer/js/taskExecution.js`：增加执行类型和时间选择
- `src/renderer/js/commandExecution.js`：增加执行类型和时间选择
- `src/renderer/js/navigation.js`：注册"任务调度"页面路由
- `src/main/main.js`：初始化定时任务调度器
- `src/renderer/js/services/taskService.js`：增加定时任务相关API

### 界面细节与交互优化
1. **时间选择器**：使用 HTML5 原生 `<input type="datetime-local">` 控件，简单高效
2. **时间验证**：JavaScript 验证不允许选择过去的时间，设置 min 属性为当前时间
3. **默认时间**：打开时间选择器时，默认设置为当前时间 + 1小时
4. **按钮文案保持不变**：统一使用"开始执行"按钮，后端根据是否勾选定时执行判断行为
5. **成功提示区分**：
   - 立即执行："任务执行中..."
   - 定时执行："定时任务已创建，将在 YYYY-MM-DD HH:mm:ss 自动执行"

### 用户体验优化
1. **倒计时显示**：在任务调度页面实时显示"距离执行还剩 X 小时 Y 分钟"
2. **执行提醒**：任务执行前可选发送系统通知（Electron Notification）（后期优化）
3. **任务日志**：定时任务执行完成后，在执行历史中明确标注"定时任务"
4. **任务复制**：支持基于已有任务快速创建新的定时任务（后期优化）

## 2025年12月01日 
- 脚本管理列表 默认排序以 更新时间倒叙，最新修改的脚本显示在最上面
- 脚本管理再新增一个复制脚本的功能，点击复制自动创建一个脚本副本

## 20251215
1、在执行命令时候没有返回得时候,会识别成执行失败;有结果返回得时候是执行成功得，优化一下这个情况，实际命令应该是执行成功得，只是终端没有返回结果（预期内），避免产生歧义。

**解决方案：**
- 在 sshService.js 中增加过滤命令无匹配结果的检测逻辑（grep/awk/sed 等）
- 当满足以下条件时，将退出码 1 视为正常执行：
  - 是过滤命令（包含 grep/egrep/fgrep/awk/sed）
  - 有执行跟踪输出（bash -x 产生的 stderr）
  - 无标准输出
  - 退出码为 1
- 前端显示优化：
  - 成功日志显示为“✓ 执行成功，无匹配结果”
  - 添加友好提示：“(过滤条件无匹配结果)”
  - 显示提示信息：“提示: 命令执行成功，过滤条件无匹配结果（这是正常情况）”
- 修改文件：
  - src/main/sshService.js
  - src/renderer/js/commandExecution.js
  - src/renderer/js/taskExecution.js
  
**状态：✅ 已完成**

**执行示例：**
[17:28:27] 开始执行临时命令任务: 临时命令_20251215_172827
[17:28:27] 目标主机: 46 台
[17:28:27] 并发数: 1, 超时时间: 300秒
[17:28:32] 172.16.8.82 - 开始执行...
[17:28:32] 172.16.8.165 - 开始执行...
[17:28:32] 172.16.8.79 - 开始执行...
[17:28:32] 172.16.8.161 - 开始执行...
[17:28:32] 172.16.8.86 - 开始执行...
[17:28:32] 172.16.8.87 - 开始执行...
[17:28:32] 172.16.8.105 - 开始执行...
[17:28:32] 172.16.8.106 - 开始执行...
[17:28:32] 172.16.8.153 - 开始执行...
[17:28:32] 172.16.8.157 - 开始执行...
[17:28:32] 172.16.8.105 - ✗ 执行失败 (退出码: 1)
┌─ 错误输出 (172.16.8.105) ─────
│ + cat /opt/app/uas-trade/logs/uas-trade.2025-12-15.0.log
│ + grep ERROR
│ + grep -v com.sinolink.tcp.CommEventReqBizHandler
│ + grep 2181
└──────────────────────────────────────────────────
提示: 退出码 1 通常表示通用错误或命令执行失败
[17:28:32] 172.16.8.47 - 开始执行...
[17:28:32] 172.16.8.157 - ✗ 执行失败 (退出码: 1)
┌─ 错误输出 (172.16.8.157) ─────
│ + cat /opt/app/uas-trade/logs/uas-trade.2025-12-15.0.log
│ + grep ERROR
│ + grep -v com.sinolink.tcp.CommEventReqBizHandler
│ + grep 2181
└──────────────────────────────────────────────────
提示: 退出码 1 通常表示通用错误或命令执行失败
[17:28:32] 172.16.8.48 - 开始执行...
[17:28:32] 172.16.8.153 - ✗ 执行失败 (退出码: 1)
┌─ 错误输出 (172.16.8.153) ─────
│ + cat /opt/app/uas-trade/logs/uas-trade.2025-12-15.0.log
│ + grep ERROR
│ + grep -v com.sinolink.tcp.CommEventReqBizHandler
│ + grep 2181
└──────────────────────────────────────────────────
提示: 退出码 1 通常表示通用错误或命令执行失败
[17:28:33] 172.16.8.239 - 开始执行...
[17:28:33] 172.16.8.106 - ✓ 执行成功 (耗时: 936ms)
┌─ 执行过程 (172.16.8.106) ─────
│ + cat /opt/app/uas-trade/logs/uas-trade.2025-12-15.0.log
│ + grep ERROR
│ + grep -v com.sinolink.tcp.CommEventReqBizHandler
│ + grep 2181
└──────────────────────────────────────────────────
┌─ 标准输出 (172.16.8.106) ─────
│ 2025-12-15 10:16:36.025 [pool-8-thread-1] ERROR c.h.macs.shares.interfaces.channel.ClientChannel 326 - channel is closed . clientChannel is =ClientChannel [mChannelId=1564221108415238144, uid=1532112128071524352, closeCause=正常, handInfo=HandInfo{version='200', clientType='OpenHarmony-6.0.1.115', cerSerialNo='uas-server-v10.01', randomIndexHead='5254327218155291', randomDictionary='2T991EYPJ5WQIZX6GPVZG', randomIndexTail='', randomIndexFactor='', finishHand=true, shareKey='6ZTTTTP99QEWQ5TW', keyLength=16, clientSocketUid='1532112128071524352', clientSocketSeq='976'}]
└──────────────────────────────────────────────────
[17:28:33] 172.16.8.244 - 开始执行...
[17:28:33] 172.16.8.79 - ✗ 执行失败 (退出码: 1)
┌─ 错误输出 (172.16.8.79) ─────
│ + cat /opt/app/uas-trade/logs/uas-trade.2025-12-15.0.log
│ + grep ERROR
│ + grep -v com.sinolink.tcp.CommEventReqBizHandler
│ + grep 2181
└──────────────────────────────────────────────────
提示: 退出码 1 通常表示通用错误或命令执行失败
[17:28:33] 172.16.28.180 - 开始执行...
[17:28:33] 172.16.8.86 - ✗ 执行失败 (退出码: 1)
┌─ 错误输出 (172.16.8.86) ─────
│ + cat /opt/app/uas-trade/logs/uas-trade.2025-12-15.0.log
│ + grep ERROR
│ + grep -v com.sinolink.tcp.CommEventReqBizHandler
│ + grep 2181
└──────────────────────────────────────────────────


你好！我是 Gemini。作为你的强力 AI 助手，我已经深入剖析了你提供的后端代码（Electron Main Process + Node.js Services）。

这份代码构建了一个基于 Electron 的**批量 SSH 任务执行工具**，具备主机管理、脚本分发、定时任务和文件上传功能。整体架构逻辑清晰，模块分工明确，但在**安全性、稳定性、性能**以及**功能完备性**上存在一些显著的改进空间。

以下是详细的代码分析报告：

---

### 📑 第一部分：Bug 与 潜在风险分析 (Critical & Major)

这些问题可能会导致程序崩溃、安全漏洞或逻辑错误，建议优先修复。

#### 1. 致命的安全配置 (Electron Security)

* **位置**: `main.js` -> `createWindow`
* **问题**:
```javascript
webPreferences: {
  nodeIntegration: true, // ⚠️ 极度危险
  contextIsolation: false, // ⚠️ 极度危险
  enableRemoteModule: true // ⚠️ 已废弃且危险
}

```


* **分析**: 开启 `nodeIntegration` 且关闭 `contextIsolation` 意味着渲染层（前端页面）可以直接调用 Node.js API。虽然这是内部工具，但如果加载了恶意脚本或被 XSS 攻击，攻击者可以直接控制用户的操作系统。
* **建议**: 迁移到 `contextBridge` 和 `preload.js` 模式，仅暴露必要的 IPC API 给渲染进程。

#### 2. SSH 脚本执行的 Heredoc 分隔符冲突

* **位置**: `sshService.js` -> `executeScript`
* **问题**:
```javascript
await connection.execCommand(`cat > ${remoteScriptPath} << 'EOF_SCRIPT_CONTENT'
${scriptContent}
EOF_SCRIPT_CONTENT`);

```


* **分析**: 代码硬编码了 `EOF_SCRIPT_CONTENT` 作为结束符。如果用户编写的 `scriptContent` 内部恰好也包含了这一行字符串（虽然概率低，但作为通用工具必须考虑），会导致脚本截断或写入失败，甚至注入恶意命令。
* **建议**: 生成一个随机的界定符（例如 `EOF_${UUID}`）来替代硬编码的字符串。

#### 3. 重试机制未实现 (功能缺失)

* **位置**: `taskExecutor.js`
* **问题**: 构造函数中定义了 `this.retryTimes = options.retryTimes || 3;`，但在 `executeBatch` 或 `executeOne` 的逻辑中，**并没有使用这个参数**。
* **分析**: `executeOne` 函数内部只有一个 `try-catch`，捕获异常后直接标记为失败，没有循环重试的逻辑。配置了重试次数却不生效，会误导用户。
* **建议**: 在 `executeOne` 内部包裹一层重试循环。

#### 4. 任务取消逻辑不彻底 (僵尸进程风险)

* **位置**: `taskExecutor.js` -> `stopTask` 和 `executeBatch`
* **问题**: 点击停止任务时，代码只是通过 `stopFlags` 停止了**后续**的调度，并修改了数据库状态。
* **分析**: 对于**正在运行中**的 SSH 命令，代码并没有发送信号去 terminate 远程进程。这意味着如果用户在一个长耗时脚本执行中点击“停止”，前端显示已停止，但服务器后台还在跑，可能导致资源占用或数据损坏。
* **建议**: `SSHService` 需要暴露 `dispose` 或 `kill` 方法，`TaskExecutor` 需要维护 `taskId` 到 `connection` 的映射，取消时必须断开 SSH 连接（这通常会发送 SIGHUP 给远程进程）。

#### 5. 定时任务调度逻辑缺陷

* **位置**: `taskScheduler.js` -> `shouldExecute`
* **问题**: `return scheduledTime <= now;`
* **分析**: 如果服务重启，或者因为某些原因调度器暂停了一段时间，重启后所有 `scheduledTime` 在过去的任务都会瞬间被触发执行。这可能导致“过期任务”的雪崩效应。
* **建议**: 增加状态检查，或者只执行“过去 X 分钟内”的任务，过早的任务标记为“过期/错过”。

---

### 🚀 第二部分：性能优化点 (Optimization)

这些点不影响功能，但严重影响效率和用户体验。

#### 1. SSH 连接复用 (Connection Pooling) - **核心性能瓶颈**

* **现状**: 目前代码中，**每一次**操作（测试连接、收集信息、执行命令、上传文件）都会 `new NodeSSH()` -> `connect` -> `dispose`。
* **影响**: 建立 SSH 连接（TCP握手 + 加密协商 + 认证）是非常耗时的（通常 1-3秒）。如果批量对同一台主机执行多个步骤，效率极低。
* **优化**: 实现连接池。在 `TaskExecutor` 批量执行期间，对同一台 Host 保持一个长连接，直到所有指令执行完毕再断开。

#### 2. 临时文件清理的原子性

* **现状**: `executeScript` 先 `cat` 写入，再 `bash` 执行，最后 `rm`。
* **优化**: 建议使用 `trap` 命令在 Shell 脚本层面保证清理，或者使用 `ssh.execCommand` 直接传递 stdin（如果有对应 API 支持），避免落盘。如果必须落盘，建议使用 `mktemp` 命令生成文件名，而不是在 JS 里拼凑路径，更安全。

#### 3. 内存与日志处理

* **现状**: 任务执行的 `stdout` 和 `stderr` 是完全缓存在内存中的，并且通过 IPC 完整发送给前端。
* **影响**: 如果是一个产生大量日志的脚本（例如编译过程或长日志查看），Electron 进程可能会 OOM (Out of Memory) 或界面卡死。
* **优化**: 限制日志存储大小（例如只存最后 1MB），或者使用流式传输（Stream）将日志写入文件，前端只按需读取。

#### 4. 并发控制逻辑

* **现状**: `executeBatch` 中手动维护 `executing` Set 和 `Promise.race`。
* **优化**: 代码逻辑尚可，但建议使用成熟库如 `p-limit` 或 `p-queue`。它们经过战验，能更好地处理并发、暂停、继续和优先级。

---

### 💡 第三部分：新的需求点与功能建议 (New Features)

基于当前代码库，以下功能可以让你的工具从“能用”变成“神器”：

#### 1. 交互式 Web Terminal (WebTTY)

* **痛点**: 目前只能执行预设脚本，查看静态输出。用户经常需要手动 SSH 上去调试。
* **方案**: 集成 `xterm.js` (前端) + `node-pty` / `ssh2` stream (后端)，在 Electron 内部提供一个真正的交互式终端 Tab。

#### 2. 敏感数据加密存储 (Secret Management)

* **痛点**: 代码中 HostModel 似乎直接存储了 `password` 或 `privateKey`。
* **方案**: 使用 Electron 的 `safeStorage` API（基于系统的 Keychain/DPAPI）来加密存储密码和私钥。不要明文存数据库。

#### 3. Ansible/Playbook 模式支持

* **痛点**: 目前只能执行单一 Shell 脚本。复杂的运维场景需要多步骤（先传文件，再解压，再改配置，最后重启）。
* **方案**: 引入“工作流”概念，允许用户编排多个原子任务：`[上传文件] -> [执行脚本 A] -> [检查结果] -> [执行脚本 B]`。

#### 4. 实时监控大盘 (Dashboard)

* **痛点**: 只有任务列表。
* **方案**: 利用 `sshService.collectHostInfo` 的能力，做一个定时轮询，展示所有主机的 CPU、内存、磁盘水位的实时仪表盘。

#### 5. 审计日志 (Audit Log)

* **痛点**: 谁在什么时候执行了什么危险命令？
* **方案**: 记录所有操作的历史记录，不可删除，用于事后追溯。特别是在团队共用此工具时非常重要。

#### 6. 文件分发的高级模式

* **痛点**: 目前是 SCP 模式。
* **方案**: 增加 `rsync` 支持（如果系统有安装），支持增量同步，大幅提高大文件夹的传输速度。

---

### 📝 总结

这份代码完成度较高，基本逻辑闭环。

* **当前评分**: 70/100
* **主要扣分项**: Electron 安全配置极其危险、SSH 连接未复用导致效率低、重试逻辑未实装。

如果你需要，我可以为你提供 **修复 `nodeIntegration` 安全漏洞的代码模板** 或者 **实现 SSH 连接池的重构方案**。请告诉我你最想先解决哪一个？



你好！我是 Gemini。已经接收到你上传的前端代码文件。结合之前分析的后端代码（Electron Main Process），我对这套前端代码（Renderer Process）进行了深度审计。

前端代码主要采用原生 JavaScript (Vanilla JS) 编写，配合 Electron 的 IPC 通信。这种写法虽然轻量，但在状态管理、DOM 操作性能和安全性上容易留下隐患。

以下是详细的分析报告：

### 📑 第一部分：严重隐患与 Bug (Critical & Major)

这些问题可能导致页面卡死、内存泄漏或逻辑错误。

#### 1. DOM 操作性能灾难 (Task Log Rendering) - ✅ 已修复

* **推测位置**: `taskExecution.js` 或 `commandExecution.js` (处理 `task:progress` 事件时)
* **现象**: 在接收 SSH 输出日志时，如果使用了类似 `logContainer.innerHTML += newLog` 或 `logContainer.append(newLogDiv)` 的方式，且没有进行**虚拟滚动 (Virtual Scrolling)** 或 **DOM 节点限制**。
* **后果**: 当 SSH 任务输出大量日志（如编译过程、长屏输出）时，DOM 节点数量会激增（成千上万个 `<div>`）。这会导致：
* 页面帧率 (FPS) 暴跌，界面卡顿。
* Electron 渲染进程内存占用飙升，甚至崩溃 (OOM)。


* **修复方案** (已实施):
* 实现**最大行数限制**，只保留最近的 1000 行，旧的日志从 DOM 中移除：
```javascript
while (logContainer.children.length > 1000) {
    logContainer.removeChild(logContainer.firstChild);
}

```
* **修复范围**:
  - `src/renderer/js/taskExecution.js` - `addLog()` 函数
  - `src/renderer/js/commandExecution.js` - `addLog()` 函数
  - `src/renderer/js/fileUpload.js` - `appendLog()` 函数
* **效果**: 防止大量日志输出时 DOM 节点过多导致的页面卡顿和内存溢出问题。






#### 2. IPC 监听器泄漏 (Event Listener Leaks)

* **推测位置**: `taskHistory.js`, `taskExecution.js`
* **问题**: 在 Electron 中，`ipcRenderer.on` 如果在组件（或视图）多次初始化时被重复调用，且没有对应的 `removeListener`，会导致监听器叠加。
* **场景**: 用户在“任务历史”和“执行任务” Tab 之间来回切换。如果每次切换到“历史” Tab 都在 `init()` 里注册一遍 `ipcRenderer.on('task-ended', ...)`。
* **后果**: 接收到一次后端事件，前端会执行多次回调。可能导致：
* 同一条日志被打印多次。
* 内存泄漏。


* **修复建议**:
* 使用 `ipcRenderer.invoke` (Promise 风格) 代替 `send/on` 模式，`invoke` 是单次请求响应，不存在监听器泄漏问题。



#### 3. 安全性：XSS 注入风险

* **推测位置**: `utils.js` (格式化函数) 或日志渲染处
* **问题**: 如果后端返回的日志内容（`stdout`/`stderr`）包含了 HTML 标签（例如脚本输出了 `<img src=x onerror=alert(1)>`），而前端使用了 `innerHTML` 直接渲染。
* **后果**: 虽然是本地工具，但如果攻击者能控制远程服务器的输出，就能在你的工具里执行恶意 JS 代码。
* **修复建议**:
* 始终使用 `textContent` 或 `innerText` 设置文本。
* 如果必须解析 ANSI 颜色转 HTML，请使用专门的库（如 `ansi-to-html`）并配合 DOMPurify 进行清洗。



---

### 🚀 第二部分：功能逻辑缺陷与 UX 优化点

#### 1. 缺乏“加载中”状态 (Loading States)

* **位置**: `fileUpload.js`, `taskExecution.js`, `scriptManagement.js`
* **问题**: 在点击“上传”、“执行”或“保存”后，到后端返回结果之间，界面似乎没有禁用按钮或显示 Spinner。
* **后果**: 用户可能因为没看到反应而狂点按钮，导致重复提交任务或重复上传文件。
* **优化**:
```javascript
async function handleUpload() {
    const btn = document.getElementById('uploadBtn');
    btn.disabled = true;
    btn.innerText = '上传中...';
    try {
        await ipcRenderer.invoke('execute-file-upload', ...);
    } finally {
        btn.disabled = false;
        btn.innerText = '上传';
    }
}

```



#### 2. 表单状态残留

* **位置**: `scriptManagement.js` (模态框逻辑)
* **问题**: 点击“编辑”脚本 A，修改了内容但点了“取消”。再次点击“新建”脚本，模态框里可能还残留着脚本 A 的数据。
* **优化**: 在打开模态框的函数中，必须显式重置表单：`form.reset()` 或手动清空所有 input value。

#### 3. 文件上传的并发限制与进度条

* **位置**: `fileUpload.js`
* **问题**: 后端代码显示支持多主机上传。前端是否展示了**总体进度** vs **单机进度**？如果只显示一个总进度条，用户无法知道是哪台机器卡住了。
* **优化**: 建议设计一个列表视图，显示每台目标主机的上传状态（等待、上传中 xx%、完成、失败）。

#### 4. 脚本编辑器体验差

* **位置**: `scriptManagement.js`
* **问题**: 很有可能使用的是 `<textarea>` 来编写 Shell 脚本。
* **优化**: 集成 **Monaco Editor** (VS Code 的核心) 或 **CodeMirror**。这能提供：
* Shell 语法高亮 (Syntax Highlighting)。
* 行号显示。
* 自动补全。
* 这对于运维工具来说是核心体验升级。



---

### 🛠️ 第三部分：代码质量与架构建议

#### 1. 模块化缺失 (Global Scope Pollution)

* **现状**: 看起来这些 JS 文件被 `<script>` 标签引入到 `index.html` 中。这意味着定义的函数和变量都在 `window` 全局作用域下。
* **风险**: 变量名冲突（例如 `utils.js` 和 `components.js` 可能都定义了 `function init()`）。
* **建议**:
* 短期：使用 IIFE (立即执行函数) 包裹每个文件代码：`(() => { ... })()`。
* 长期：使用 Webpack/Vite 等打包工具，利用 ES Modules (`import`/`export`) 进行开发。



#### 2. 硬编码的 IPC 通道名

* **现状**: `ipcRenderer.send('task:create', ...)` 字符串散落在各个文件中。
* **建议**: 创建一个 `constants.js` 文件，统一定义所有通道名称。
```javascript
// constants.js
export const IPC_CHANNELS = {
    TASK_CREATE: 'task:create',
    TASK_PROGRESS: 'task:progress',
    // ...
};

```


这样后端和前端修改通道名时更安全。

---

### ✨ 第四部分：推荐的新功能 (New Requirements)

1. **暗黑模式 (Dark Mode)**:
* 运维人员通常喜欢深色界面。前端可以通过 CSS Variables (`var(--bg-color)`) 轻松实现一键切换主题。


2. **快捷命令面板 (Quick Command Palette)**:
* 类似 VS Code 的 `Ctrl+P`。前端监听键盘事件，弹出一个搜索框，可以快速搜索主机、搜索脚本并立即执行，无需鼠标点击。


3. **日志的高级搜索与过滤**:
* 在任务执行页面，增加一个“前端搜索框”。允许用户在已输出的日志中输入关键字（如 `Error`），高亮显示所有匹配行。


4. **连接状态指示器**:
* 在界面角落增加一个指示器，显示当前与后端（Main Process）的连接状态，或者显示后端 SSH 连接池的活跃数量。



### 📊 总结

前端代码目前处于“功能实现”阶段，但在**大规模数据渲染**（日志）和**交互细节**上存在短板。

**建议优先修复顺序：**

1. **Task Log Rendering**: 解决日志输出卡顿问题（引入 xterm.js 或 DOM 节点限制）。
2. **IPC Leak**: 检查并修复重复监听器。
3. **Loading UX**: 给所有异步操作加上禁用/加载状态。
4. **Editor**: 将 `<textarea>` 升级为代码编辑器组件。

你需要我为你提供 **xterm.js 集成到现有项目的代码示例**，还是 **Monaco Editor 的配置代码**？

需求沟通，在脚本执行的 选择执行脚本的时候，否能也增加一个查看脚本内容的功能，因为有时候执行前还是想确认一下执行内容，这样不用切换到脚本管理里面在找脚本点击查看按钮了，看下需求是否理解清楚。


需求沟通，在执行历史页面，目前有状态筛选功能，再新增一个任务类型的筛选功能。看下需求是否理解清楚。

## 主机管理需求
1、需求沟通，目前的导入只能新增主机是吗？其他的修改项是无法导入的是吗？
如果我有主机需要密码或者其他信息想通过excel 批量完成修改后导入好像不能？看下需求是否能实现？
2、主机管理页面，筛选后筛选框会变成绿色，可是当清除筛选、刷新列表 后被操作过的筛选框还是绿色的，这个颜色也能恢复原先的状态吗？
3、需求沟通，有些主机管理 配置的部署root 账户，但是有时候执行脚本的时候需要用sudo 权限去执行。所以希望在脚本执行的时候增加一个用sudo 执行脚本的选项，如果已经是root用户用sudo是不是没有问题？需求是否沟通清楚？

前端页面提示 

使用sudo执行💡
💡 提示：所有系统需配置免密sudo，root用户执行sudo命令不会有问题

改为

使用sudo执行💡（提示：系统需配置免密sudo）

#导入需求
目前导出的需求，是这些字段
IP地址	端口	用户名	密码	描述	环境	系统名称	应用名称	机房	负责人
支持的导入的字段是什么？

#20260116需求
需求沟通
1、脚本执行、命令执行、文件上传、三个页面的列表排序现在是最新的任务在最下面显示，改为倒叙，最新执行过的任务在第一个显示。
2、脚本执行列表新增一列，脚本名称展示。
3、脚本执行页面里面不用显示 命令执行、文件上传的记录。
需求是否理解清楚

## 2026年01月21日
1、主机管理页面，批量选中主机后，支持批量连接测试。
   - 增加 Ping (ICMP) 和 SSH (Port 22) 两种测试方式选项。
   - 增加导出 Excel 功能，方便导出测试结果（包含 IP、主机名、系统名称、机房、环境、负责人、测试方式、测试状态、详细详情/延迟、测试时间）。
2、修复批量 Ping 测试导出 Excel 时，基础信息（IP、主机名、负责人等）显示为“未知”或为空的问题。
   - 优化了 BatchOperationDialog 的执行逻辑，确保所有类型的探测结果均完整关联主机元数据。
3、需求沟通，批量测试完成后，实时同步更新测试结果到主机管理页面的状态属性中（持久化到数据库）。
   - 优化了 hostService.updateHostStatus 方法，确保在 Electron 环境下能正确调用 IPC 更新数据库。
   - 改进了批量探测逻辑，Ping 探测成功也会将主机状态更新为“在线”。

## 20260203
C:\Users\andy\AppData\Roaming\ssh-tools-box
C:\Users\yjbmaintain\AppData\Roaming\ssh-tools-box

C:\Users\andy\AppData\Roaming\ssh-tools-box\data\database.json

/ssh-tools-box
$ du -sh *
5.3M    Cache
4.0K    Code Cache
548K    DawnCache
444K    Dictionaries
548K    GPUCache
1.0K    Local State
127K    Local Storage
41K     Network
1.0K    Preferences
9.0K    Session Storage
0       backups
0       blob_storage
0       config
93M     data
0       logs
8.0K    ssh_keys
0       updates

数据库初始化竞态：init.js 执行迁移时，若 main.js 过早创建窗口并触发数据读取，可能导致“Table not found”错误。

心跳检测频率：connectionMonitor.js 的频率固定，在弱网环境下可能产生大量的假报警信息。


1. SSH 连接释放不彻底（内存与句柄泄露）
在 sshService.js 中，虽然你监听了 close 和 error 事件，但存在异步竞争风险。

隐藏风险：当网络出现闪断或 SSH 超时触发时，如果 stream 已经销毁但 conn 仍在尝试重连或握手，某些回调函数可能依然在闭包中持有对象引用。

后果：随着运行时间增长，应用占用的内存会持续上升。

建议方案：在 sshService 中引入一个强力的 cleanup 函数，确保在销毁连接时显式执行 conn.removeAllListeners()。

2. 数据库初始化与 IPC 请求的竞态条件
在 main.js 和 init.js 的配合中存在时序问题。

隐藏风险：Electron 的窗口加载（loadURL）几乎与主进程初始化同步进行。如果数据库迁移（migrate_logs.js）或初始化（init.js）尚未完成，而渲染进程的 app.js 已经发起了 hosts:load 的 IPC 请求，程序会因尝试访问不存在的表或未连接的数据库而抛出致命错误。

后果：用户偶尔会遇到“白屏”或启动时报错，且难以复现。

建议方案：在 main.js 中，务必使用 await db.init() 确保数据库准备就绪后再调用 createWindow()。

3. 文件上传的"目录路径陷阱" ✅ 已修复
在 fileUpload.js 处理目标路径时。

隐藏风险：你允许用户手动输入 targetPath。如果用户输入了 /home/user/test（不带结尾斜杠），而目标主机上 test 是一个已存在的文件而非目录，上传逻辑会报错甚至覆盖掉该非目录文件。

**已实现方案（修复时间：2026-02-04）**
- 在 sshService.js 的 uploadFile 方法中，在执行 SFTP 上传前增加了路径类型检查
- 使用 shell 条件判断 (if [ -f ] elif [ -d ]) 来区分目标路径是文件、目录还是不存在
- 如果目标路径是已存在的文件，抛出明确的错误提示，防止误操作
- 如果目标路径不存在，根据 pathCreateStrategy 配置决定是否自动创建或报错
- 如果目标路径是目录，正常继续上传流程
- 优化了错误提示信息，给用户提供明确的操作建议

修复位置：`src/main/sshService.js` L929-L954

4. 任务调度器的"时间跳变" Bug ✅ 已采用建议方案
在 taskScheduler.js 中，定时任务依赖于本地内存计时器。

隐藏风险：如果用户在任务排队期间修改了系统时间（这在金融测试环境同步时间戳时常发生），或者应用在任务触发点前瞬间被操作系统挂起（休眠），计时器回调可能失效或延迟触发。

**已实现方案（验证时间：2026-02-04）**
当前实现已采用"状态轮询"模式，具体实现：

1. **定时轮询**：使用 `setInterval` 每60秒查询一次数据库，而非依赖单次 `setTimeout` 计算延迟
2. **数据库驱动**：每次轮询查询 `status = 'scheduled' AND scheduled_time <= NOW()` 的任务
3. **实时对比**：每次都用当前系统时间与数据库中的 `scheduled_time` 对比，不受时间跳变影响
4. **防重复执行**：使用 `executedTasks` Set 记录已执行任务，防止同一轮询周期内重复触发
5. **过期处理**：超过阈值（1小时）的任务标记为 `missed`，避免在唤醒后并发执行大量过期任务

**优点**：
- 抵抗系统时间修改：每次轮询都重新获取当前时间，不依赖历史计算
- 抵抗系统休眠：唤醒后自动恢复轮询，不会丢失任务
- 防雪崩：过期任务自动标记为错过，不会瞬间并发执行

实现位置：`src/main/taskScheduler.js` L10（检查间隔）、L37-39（轮询逻辑）、L57-86（任务检查）

5. IPC 数据传输的“大对象阻塞”
在渲染进程 hostService.js 调用全量主机列表时。

隐藏风险：IPC 通道在 Electron 中是单线程序列化的。如果你有 2000 台以上的主机，每台主机包含大量的备注信息和配置，一次性 ipcRenderer.invoke('hosts:load') 会导致渲染进程 UI 线程阻塞几百毫秒。

后果：点击“主机管理”标签时，界面会出现明显的卡顿感。

建议方案：在后端 hostService.js 实现分页查询，前端只请求当前可视区域的数据。

1. AI 驱动的自动化与辅助决策
既然你已经有“AI拓扑图生成助手”的开发经验，将 AI 深度集成到核心流程中是必然趋势。

AI 智能命令补全与解释：集成大模型，根据当前选中的主机系统（如 CentOS 7.4），在输入命令时提供智能预测，并实时解释复杂 Shell 脚本的功能。

异常日志智能审计：在任务执行结果（taskHistory.js）中，利用 AI 自动分析执行失败的原因（如网络延迟导致的 Intrinsic Latency 波动或磁盘 Invalid argument 错误），并给出修复建议。

自然语言转运维脚本：允许用户在“脚本管理”中通过输入“帮我写一个检查所有主机 CPU 窃取时间（Steal Time）并输出到日志的脚本”来直接生成代码。

2. 金融级安全性与合规性增强
针对金融行业的严苛合规要求，安全性迭代是你的加分项。

操作回放与录屏：除了文字日志（logService.js），为命令执行（commandExecution.js）增加 ASCII 录制功能（如 asciinema 格式），实现运维操作的全过程审计回溯。

多因素身份验证 (MFA)：在“设置”中增加连接保护，连接重要生产环境主机前，需通过手机验证码或 TOTP（如 Google Authenticator）进行二次确认。

敏感指令拦截（WAF for SSH）：建立动态黑名单，在主进程 sshService.js 层面拦截诸如 rm -rf /、mkfs 等高危指令，并支持基于正则表达式的指令白名单过滤。

3. 深度可视化与监控集成
利用你已有的 netstat 转 Mermaid 拓扑图逻辑，将静态工具升级为动态平台。

实时性能监控看板：将 connectionMonitor.js 扩展为轻量级监控监控插件，直接在 UI 上以小图表形式展示主机的 CPU、内存和 Steal Time 趋势，无需手动执行脚本。

动态拓扑同步：在“主机管理”中，一键扫描当前所有选中的主机连接关系，自动绘制出多机房（Datacenter）高可用架构图。

4. 批量操作的高级自动化流
将单点的脚本执行（taskExecution.js）升级为复杂的工作流。

工作流编排（DAG）：支持将多个脚本（Script）串联成任务流。例如：步骤1检查环境 -> 步骤2上传文件 -> 步骤3执行安装 -> 步骤4验证状态。只有前一步成功，才继续下一步。

配置基线检查（Compliance As Code）：利用你现有的 baseline_check.sh，建立一键基线扫描功能，对比当前环境与金融行业安全标准的差异并生成报告。

## 20260305 - 工程任务功能需求

### 功能概述
基于现有的脚本执行、命令执行、文件上传 3 个核心功能，迭代出**工程任务**功能，支持将多个操作编排成一个完整的工程化流程。

### 核心功能

#### 1. 工程任务管理
- **创建工程**：自定义工程名称、描述
- **步骤编排**：支持添加、删除、拖拽排序步骤
- **步骤类型**：
  - 文件上传（复用现有文件上传功能）
  - 命令执行（复用现有命令执行功能）
  - 脚本执行（复用现有脚本执行功能）
- **目标主机**：每个步骤独立选择目标主机（支持主机组/标签筛选）

#### 2. 执行控制策略
采用**严格模式（失败即停）**：
- 任一主机执行失败，整个步骤失败
- **任一步骤失败，整个工程立即中止，后续所有步骤不再执行**
- 并行的步骤中如有任一失败，其他正在执行的步骤也立即停止
- 记录失败原因，发送失败通知

#### 3. 步骤依赖关系
支持自定义步骤依赖，实现灵活的串并行执行：

**示例流程**：
```
步骤1: 上传程序包 → 交易集群 (无依赖)
    ↓
步骤2: 停止服务 → 交易集群 (依赖步骤1)
    ↓
步骤3: 备份配置 → 交易集群 (依赖步骤1，与步骤2并行)
    ↓
步骤4: 启动服务 → 交易集群 (依赖步骤3)
```

**依赖规则**：
- 步骤可以指定依赖一个或多个前置步骤
- 所有依赖步骤完成后，当前步骤才能开始
- 任一依赖步骤失败，当前步骤取消执行

#### 4. 执行模板
- 保存常用工程为模板
- 基于模板快速创建新工程
- 模板包含：步骤配置、依赖关系、通知设置

#### 5. 定时执行
- 支持设定执行时间（类似现有定时任务功能）
- 到达设定时间自动触发工程执行
- 支持取消待执行的定时工程

#### 6. 企业微信通知
- **通知时机**：执行成功、执行失败、执行中断
- **通知内容**：
  - 工程名称
  - 执行状态
  - 执行时长
  - 步骤统计（成功/失败/总步骤）
  - 失败步骤详情（如有）

### 数据库设计

```sql
-- 工程任务表
CREATE TABLE project_tasks (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    name TEXT NOT NULL,                    -- 工程名称
    description TEXT,                       -- 工程描述
    is_template INTEGER DEFAULT 0,          -- 是否为模板
    notify_webhook TEXT,                    -- 企业微信Webhook地址
    notify_on_success INTEGER DEFAULT 1,    -- 成功时通知
    notify_on_failure INTEGER DEFAULT 1,    -- 失败时通知
    notify_on_interrupt INTEGER DEFAULT 1,  -- 中断时通知
    created_at TEXT,
    updated_at TEXT
);

-- 工程步骤表
CREATE TABLE project_steps (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    project_task_id INTEGER,
    step_order INTEGER,                     -- 显示顺序
    step_name TEXT,                         -- 步骤名称
    step_type TEXT,                         -- 步骤类型: upload/command/script
    host_ids TEXT,                          -- 目标主机ID列表（JSON）
    config TEXT,                            -- 步骤配置（JSON，根据类型不同）
    depends_on TEXT DEFAULT '[]',           -- 依赖步骤ID列表（JSON）
    timeout INTEGER DEFAULT 300,            -- 超时时间（秒）
    FOREIGN KEY (project_task_id) REFERENCES project_tasks(id)
);

-- 工程执行记录表
CREATE TABLE project_executions (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    project_task_id INTEGER,
    status TEXT,                            -- pending/running/completed/failed/interrupted
    execution_type TEXT DEFAULT 'manual',   -- 执行类型: manual(手动)/scheduled(定时)
    scheduled_time TEXT,                    -- 计划执行时间（定时任务用）
    started_at TEXT,
    completed_at TEXT,
    total_steps INTEGER,
    completed_steps INTEGER,
    failed_steps INTEGER,
    error_message TEXT,                     -- 失败原因
    notify_sent INTEGER DEFAULT 0,          -- 通知是否已发送
    FOREIGN KEY (project_task_id) REFERENCES project_tasks(id)
);

-- 步骤执行详情表
CREATE TABLE project_step_executions (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    project_execution_id INTEGER,
    step_id INTEGER,
    status TEXT,                            -- pending/running/completed/failed/skipped
    started_at TEXT,
    completed_at TEXT,
    host_results TEXT,                      -- 各主机执行结果（JSON）
    error_message TEXT,
    FOREIGN KEY (project_execution_id) REFERENCES project_executions(id)
);
```

### 界面规划

#### 页面1：工程任务列表
- 显示所有工程和模板
- 支持筛选：全部/模板/最近执行
- 操作：新建、编辑、删除、执行、复制

#### 页面2：工程任务编辑
- 基本信息：名称、描述
- 步骤编排区域：
  - 步骤列表（可拖拽排序）
  - 添加步骤按钮
  - 每个步骤：类型图标、名称、目标主机数、依赖关系
- 步骤配置弹窗：根据类型显示不同配置项
- 通知配置：Webhook、通知时机

#### 页面3：工程执行历史
- 显示每次执行的记录
- 执行状态、耗时、步骤完成情况
- 查看详细日志

#### 页面4：定时工程管理（复用/参考现有定时任务页面）
- 显示待执行的定时工程
- 支持修改执行时间、取消定时

### 实施阶段

**阶段一（MVP）**：
1. 数据库表创建
2. 工程任务 CRUD
3. 步骤编排（上传/命令/脚本）
4. 基础依赖执行（串行支持）
5. 执行历史记录

**阶段二**：
1. 步骤并行执行
2. 执行模板功能
3. 定时执行
4. 企业微信通知

### 执行逻辑确认 ✅

**严格模式执行规则**：
1. 步骤2和步骤3并行执行时，如果**步骤2失败**：
   - 立即停止步骤3的执行
   - 整个工程标记为失败
   - 步骤4及后续步骤不再执行

2. 步骤4依赖步骤3，如果**步骤2失败但步骤3成功**：
   - 步骤4**不执行**（因为步骤2已失败，工程已中止）

**总结**：严格模式下，工程执行是一个原子操作，任一环节失败，整个工程立即停止。

1. 执行控制策略
策略	说明	场景
失败即停	任一主机执行失败，整个步骤失败，工程中止	严格部署流程
失败继续	记录失败主机，继续执行其他主机和后续步骤	批量巡检
部分成功算成功	设置成功率阈值（如80%），超过则步骤成功	大规模灰度发布
自动重试	单主机失败自动重试N次	网络不稳定环境

2. 主机组动态绑定
不只是选择固定主机，还可以：
标签绑定：可以用 系统名称 应用名称 机房 环境 负责人
动态查询：每次执行时实时查询符合条件的主机
主机组模板：保存常用的主机筛选条件

3. 条件执行 步骤可以根据前面步骤的结果决定是否执行：

4. 执行暂停/恢复 • 执行中可以暂停（等待当前步骤完成） • 暂停后可以修改后续步骤配置 • 支持断点续传（从失败的步骤继续）

5. 执行模板 
• 保存常用工程为模板 

8. 定时执行
支持定时工程

9. 并发控制细化 
• 工程级别：同时只能执行一个实例（防止重复部署） 
• 步骤级别：控制该步骤在单主机上的并发 
• 全局级别：控制所有工程的总体并发数

#20260515
1、界面启动目前宽度是1200 吗？ 改成1300 

脚本管理页面的 导入脚本和导出脚本是 批量导入导出脚本吧，名称改下，改成批量导出、批量导入。
同时新增一个单独导入的功能，类型 .sh 脚本的，默认导入后创建时间和更新时间就是导入时间，描述和标签为空就行，看下还有哪些补充和没考虑到的地方？




脚本管理页面需求沟通

### 1. 引入分级目录（Hierarchy）

目前的平铺式列表即使有标签，在视觉上依然很重。

* **左侧树状导航**：在脚本管理页面左侧增加一个根据“业务线”或“功能模块”（如：监控、数据库、中间件、安全审计）划分的目录树。
* **按项目聚合**：你提到的“工程任务”可以与脚本管理深度关联，脚本不再是孤立的文件，而是隶属于某个特定的运维工程。


### 3. 增强标签系统的逻辑（Smart Tagging）

简单的标签容易滥用，建议将标签标准化：


* **环境标签**： 用户自己输入
* **架构标签**： 用户自己输入
* **风险等级**：高危、普通 直接选的，


* **智能筛选**： 这种组合筛选，提高检索效率。

### 5. 执行视图与管理视图分离

* **快捷面板**：在“脚本执行”页面，根据用户的使用频率或收藏，提供一个“常用工具箱”，让运维人员不需要进管理列表就能找到高频脚本。

* **全局搜索改进**：仿照 IDE（如 Cursor 或 XShell）提供全局搜索框，支持模糊匹配描述和脚本内容。

---

## 需求澄清确认（20260515）

### 1. 分级目录（Hierarchy）补充说明

- 目录层级为 **2级**（一级分类 → 二级分类），不再深入。
- 一个脚本只能归属**一个**目录；没有目录归属的脚本默认进"未分类"。
- 目录支持增删改；右侧脚本列表随选中目录联动过滤。
- 目录（归类管理） 与 标签（快速过滤）**并存，不冲突**。

### 3. 标签系统补充说明

- 标签分为 **3个固定类型**（环境、架构、风险等级），每个类型下的**具体值可自定义**增删。
- 存量脚本上已有的旧标签，统一归入**"自定义标签"**类型保留，不强制迁移。

### 5. 快捷面板补充说明

- 快捷面板为**全局共享**（当前无账户体系），由用户**手动置顶**管理，上限 **10个**。
- 全局搜索本期**暂不实现**。

### 7. .sh 脚本单独导入补充说明

- 导入时可**选择指定目录**（一级或二级），不选则默认归入"未分类"。
- 导入后创建时间/更新时间取导入时间，描述和标签为空。


需求沟通
目前 主机管理的批量连接测试效率很低 一个主机连接基本都要2s 这个速度能优化吗？ 几百台主机就非常慢了
看下需求是否理解？还有哪些没有考虑到的 ？


需求沟通
目前的脚本执行、命令执行
并发数: 1, 超时时间: 300秒
感觉机器多了效率就差了，这个目前并发和超时时间 在设置按钮中可以设置吧，
看下需求是否理解？还有哪些没有考虑到的 ？


需求沟通
目前脚本执行、命令执行的结果， 有时候只想快速高效的看到机器结果。
[23:27:17] 开始执行临时命令任务: 临时命令_20260521_232715
[23:27:17] 目标主机: 6 台
[23:27:17] 并发数: 1, 超时时间: 300秒
[23:27:24] 172.28.60.179 - 开始执行...
[23:27:24] 172.28.60.185 - 开始执行...
[23:27:24] 172.28.60.156 - 开始执行...
[23:27:24] 172.28.60.184 - 开始执行...
[23:27:24] 172.28.60.186 - 开始执行...
[23:27:24] 172.28.60.191 - 开始执行...
[23:27:27] 172.28.60.156 - ✓ 执行成功 (耗时: 628ms)
┌─ 执行过程 (172.28.60.156) ─────
│ + date
└──────────────────────────────────────────────────
┌─ 标准输出 (172.28.60.156) ─────
│ Thu May 21 23:27:25 CST 2026
└──────────────────────────────────────────────────
[23:27:27] 172.28.60.179 - ✓ 执行成功 (耗时: 654ms)
┌─ 执行过程 (172.28.60.179) ─────
│ + date
└──────────────────────────────────────────────────
┌─ 标准输出 (172.28.60.179) ─────
│ Thu May 21 23:27:18 CST 2026
└──────────────────────────────────────────────────
[23:27:27] 172.28.60.184 - ✓ 执行成功 (耗时: 652ms)
┌─ 执行过程 (172.28.60.184) ─────
│ + date
└──────────────────────────────────────────────────
┌─ 标准输出 (172.28.60.184) ─────
│ Thu May 21 23:27:26 CST 2026
└──────────────────────────────────────────────────
[23:27:27] 172.28.60.185 - ✓ 执行成功 (耗时: 671ms)
┌─ 执行过程 (172.28.60.185) ─────
│ + date
└──────────────────────────────────────────────────
┌─ 标准输出 (172.28.60.185) ─────
│ Thu May 21 23:27:18 CST 2026
└──────────────────────────────────────────────────
[23:27:27] 172.28.60.191 - ✓ 执行成功 (耗时: 695ms)
┌─ 执行过程 (172.28.60.191) ─────
│ + date
└──────────────────────────────────────────────────
┌─ 标准输出 (172.28.60.191) ─────
│ Thu May 21 23:27:18 CST 2026
└──────────────────────────────────────────────────
[23:27:27] 172.28.60.186 - ✓ 执行成功 (耗时: 723ms)
┌─ 执行过程 (172.28.60.186) ─────
│ + date
└──────────────────────────────────────────────────
┌─ 标准输出 (172.28.60.186) ─────
│ Thu May 21 23:27:25 CST 2026
└──────────────────────────────────────────────────
[23:27:29] 任务执行完成
[23:27:29] 总计: 6 台, 成功: 6 台, 失败: 0 台
但是目前 默认是会把 执行过程也全部打印出来，需要可以可视化效果更加高效的模式，可以高效看到对应主机和主机的结果，并且对结果可以快速筛选的模式，看下需求是否理解，同时还有哪些考虑不周的地方


需求沟通
左侧的快速操作，批量连接测试，直接点击会要求选择测试主机，我理解这个地方应该出现一个弹窗，然后方便勾选现有的管理主机然后就可以开始批量测试了。 就是现有的 批量连接测试 的弹窗页面，
但是主机列表这块应该改造成  脚本执行和 命令执行的 选择目标主机 一样可以快速筛选需要执行操作的主机。
看下需求是否理解，同时还有哪些考虑不周的地方？


需求沟通
主机管理里面 选中主机后 目前有 批量收集信息 批量改密钥 批量删除功能 
新增批量功能 批量改主用户名  批量改主机密码 批量改负责人 批量改系统名称
看下需求是否理解，同时还有哪些考虑不周的地方？

需求沟通
批量连接测试，ssh 成功验证了密码正确性，
ping 测试 只是表示主机在线而已，ping 测试也改变了状态字段，将两个混淆在一起了，不好区分出那些在线但是配置密码不对得机器了。
看下需求是否理解，同时还有哪些考虑不周的地方？

## 20260522 - 数据备份与导入导出功能

### 需求背景
避免因系统崩溃、磁盘损坏、误操作导致全量数据丢失，新增手动导出/导入和定时备份能力，方便用户主动备份和换机迁移。

### 决策汇总
| 项 | 决策 |
|---|---|
| 入口位置 | 右上角齿轮 → 全局设置弹窗 → 新增「💾 数据备份」分组 |
| 加密 | ❌ 不加密（明文 JSON，仅本机使用） |
| 导入策略 | 仅「完全覆盖」，不提供合并/选择性导入 |
| 定时频率 | 每天 1 次，备份时间用户自定义 |
| 存储位置 | 仅本地（`<userData>/backups/`） |
| 导入后处理 | **强制重启应用**，避免内存与磁盘不一致 |
| 导入前保护 | **强制自动备份当前数据**到 `backups/before_import/` |
| 5 分钟滚动备份 | 保留作为底层崩溃恢复兜底，UI 不展示（用户无感知） |

### UI 布局（在现有「全局设置」弹窗内追加）
```
🔧 执行参数（已有）
🔑 密钥管理（已有）
💾 数据备份                                  ← 新增
  ┌─ 手动操作 ─────────────────────────┐
  │ [📤 导出全量数据]  [📥 导入全量数据] │
  └─────────────────────────────────────┘
  ┌─ 定时备份 ─────────────────────────┐
  │ ☑ 启用每日自动备份                  │
  │   备份时间: [HH:MM]   每日该时刻执行 │
  │   保留份数: [30]      超出删除最旧   │
  │   备份目录: <userData>/backups/daily/│
  │   [打开备份目录]                    │
  └─────────────────────────────────────┘
  ┌─ 备份历史 ─────────────────────────┐
  │ 🟢 daily   2026-05-22 02:00  85.3MB [恢复][删除] │
  │ 🟡 import  2026-05-21 14:33  85.1MB [恢复][删除] │
  │ ...                                              │
  └──────────────────────────────────────────────────┘
```

### 功能细节

#### 1. 手动导出
- 文件保存对话框，默认文件名：`wxbox-backup-{机器名}-YYYYMMDD-HHmmss.json`
- 文件结构：
  ```json
  {
    "version": "1.0",
    "appVersion": "x.x.x",
    "exportedAt": "2026-05-22T10:30:00Z",
    "hostname": "ANDY-PC",
    "data": { ...完整 database.json 全部表数据 }
  }
  ```
- 大数据量（如 90MB）需显示进度，避免界面卡顿假死

#### 2. 手动导入（高危操作）
- 文件选择对话框 → 校验 `version` 兼容性（不兼容直接拒绝）
- **三重确认**：
  1. 弹窗显示数据量对比：`当前 XX 台主机/YY 脚本/ZZ 历史 → 即将变为 AA/BB/CC`
  2. 输入框二次确认（输入「确认导入」四个字）
  3. 醒目红色警告「此操作不可撤销」
- **导入前强制自动备份**当前数据到 `<userData>/backups/before_import/wxbox-before-import-YYYYMMDD-HHmmss.json`
- 导入流程：
  1. 暂停定时任务调度器、连接监控
  2. 写入新数据到 `database.json`（复用现有原子写入 + rename）
  3. **强制重启应用**（`app.relaunch() + app.exit()`）

#### 3. 定时备份
- 主进程 `setInterval(60s)`，每分钟检查：「上次备份日期 != 今天 且 当前时间 ≥ 设定时间」 → 触发备份
- `app.on('before-quit')` 补一次：当天未备份则退出前补
- 备份文件：`<userData>/backups/daily/wxbox-daily-YYYYMMDD.json`
- 保留策略：默认 30 份，超出删最旧
- 失败处理：磁盘满/路径不可写 → UI 红点角标提醒，不静默失败
- settings 新增字段：
  ```js
  backup_enabled: true,
  backup_time: "02:00",
  backup_keep_count: 30,
  backup_last_at: null   // 上次成功备份时间
  ```

#### 4. 备份历史列表
展示两类（5 分钟滚动备份不展示）：
- 🟢 daily — 每日定时备份（`backups/daily/`）
- 🟡 import — 导入前自动备份（`backups/before_import/`）

操作：
- **恢复**：复用「导入」流程（二次确认 → 备份当前 → 覆盖 → 重启）
- **删除**：单条删除
- **打开目录**：`shell.showItemInFolder` 打开文件管理器

### 涉及的文件清单

**新增**：
- `src/main/backupService.js` — 主进程备份服务（导出/导入/定时调度/历史查询）
- `src/renderer/js/services/backupService.js` — 前端 IPC 封装

**修改**：
- `src/main/main.js` — 注册 IPC、启动定时调度器、`before-quit` 补备份
- `src/database/simple-db.js` — 暴露 `getRawData()` / `replaceAllData()` 接口；DEFAULT_DATA.settings 新增备份字段
- `src/renderer/js/app.js` — `showGlobalSettings()` 增加「💾 数据备份」分组及全部交互逻辑
- `src/renderer/index.html` — 必要时调整设置弹窗宽度

### 风险与边界
- **导出文件含明文密码/私钥** — 需在导出弹窗顶部红色提示用户妥善保管
- **大文件**（>200MB）导出/导入需流式处理 + 进度条 + 取消能力
- **版本不匹配**（备份比当前 app 新）→ 拒绝并提示升级
- **重复导入同一文件** → 检测 `exportedAt` 提示用户
- **重启确认** → 重启前提示「应用将在 3 秒后重启完成导入」


# 20260527
需求沟通
主机管理里面 收集硬件信息，改下逻辑吧，要的其实是软件层面的CPU 核数、内存大小、操作系统版本，因为脚本执行、命令执行的一个重要指标就是系统版本
看下需求是否理解？还有哪些没有考虑到的？

主机管理里面 系统数据采集 要能采集到系统类型 版本 因为脚本执行的一个重要指标就是系统版本

需求沟通
1、工程任务批量执行的时候，查看执行详细，目前是需要需要滚动（相当于是日志模式），是否可以也新增和脚本执行中一样的结果模式？看下需求是否理解？还有哪些没有考虑到的？

2、脚本管理页面，编辑完成保存脚本后，页面好像会自动变成全部脚本的列表数据，但是左侧目录选择还是当前选择的目录中，保存完应该保持在之前操作的目录中，看下需求是否理解。

3、脚本管理页面，如果向下滚动了，切换了目录，滚动轴也会显示到滚动的下方，切换目录应该是滚动轴应该恢复状态。看下需求是否理解？


在结果模式下，能够突出展示那台差异的么，这样更加直观，不用在人工找到是那台


左侧的快速操作
导入 导出 excel 和批量 连接测试都移动到 主机管理页面的 按钮区域 

左侧的统计信息
全部移动整合到 底部的状态栏中

看下需求是否理解清楚？

批量ip搜索功能的窗口优化一下，目前挤在一起看起来很难受

**实施方案：**
- 弹窗固定宽度 640px（max-width: 92vw），避免弹性收缩导致布局拥挤
- 标题改用 `.modal-title` 规范样式，消除浏览器默认 h2 边距
- 分隔符说明由整段密集文字改为 kbd-chip 风格标签（逗号、分号、空格、换行、斜杠、竖线），修复“斤号”错别字（# 并不在分隔符正则内，已移除）
- 输入框加宽加高（min-height: 200px），使用 JetBrains Mono 等宽字体 + 1.7 行高，内边距更舒适
- 新增实时识别统计：输入时即时显示“批量精确匹配/模糊搜索”模式及有效、去重、非 IP 项计数
- 底部左侧显示 Ctrl+Enter 快捷键提示，右侧为操作按钮
- 修改文件：`src/renderer/js/app.js`（showBatchSearchDialog）、`src/renderer/css/components.css`（新增 .batch-sep-chip）

**状态：✅ 已完成（2026-09-12）**

批量连接测试这个窗口的内容布局不合理，不紧凑，浪费了大量的可视区域，主机列表不要卡片方式，直接最直接的列表方式展示，顶部的操作概要 也布局合理一点不要浪费空间

1、左侧的菜单宽度可以调整，合理一点不用空白那么多，把可视区域让给右侧的列表数据
2、主机管理页面的列表数据高度没有自适应，下面留了大量空白;其他页面的逻辑也一样。
3、脚本执行、命令执行、文件上传、任务调度 和其他页面右侧的风格不一样，统一右侧列表数据风格保持一致。
4、各个功能的导航名称前面不要用emoji 图标，改用专业的图标库。
5、折叠菜单的 “三” 图标应该折叠和展开动效不一样，不能打开菜单折叠菜单效果是一样的图标。


1、右侧列表展示中也不要使用emoji，也改成专业的图标。


## 20260912 - 脚本执行页脚本选择搜索式面板

脚本执行页面，脚本配置关联执行的时候，筛选功能使用不方便：脚本多了选择很乱；加了目录和标签后操作步骤更多。目标是更高效地选中脚本，界面也更好看。

**实施方案：**将「目录+标签双下拉 → 原生select长列表」替换为搜索式选择面板：
1、单一搜索框：输入即过滤（名称/描述/标签，大小写不敏感包含匹配），带匹配计数和一键清空；
2、分组列表：目录作为分组标题（sticky 吸顶），未分类置末尾，替代目录筛选下拉；
3、最近使用置顶：从执行历史提取最近使用的5个脚本，无搜索时优先展示，高频脚本单击直达；
4、标签 chip 点击过滤：行内标签可点击 toggle，激活态高亮，替代标签筛选下拉；
5、键盘导航：↑↓ 高亮、Enter 选中、Esc 清空；
6、选中摘要条：选中后显示脚本名+目录+标签及「更换」按钮；
7、`taskScriptSelect` 改为 hidden input（id 不变），startExecution/handleViewScript 等读值逻辑零改动。

- 修改文件：`src/renderer/index.html`（步骤1区域重构）、`src/renderer/js/taskExecution.js`（删除 applyScriptFilters/populateTagValueSelect/renderScriptOptions/handleScriptChange，新增 renderScriptPicker/selectScript 等约320行）、`src/renderer/css/components.css`（删除 .te-script-filters，新增 .te-script-picker 系列约230行）
- 验证：node --check 语法通过；grep 无旧引用残留；npm start 启动成功无渲染报错。

**状态：✅ 已完成（2026-09-12）**


需求沟通
1、主机管理 添加主机 录入主机信息的时候，是否可以批量一起录入，
默认主机名，就是ip地址 10个ip 一起输入，然后用户名密码都一样 机房等等其他信息都是一样的

**已实施（2026-09-12）：添加主机弹窗内置「单台录入 / 批量录入」模式切换**

交互设计：
1、仅在“添加主机”（add 模式）显示顶部 Tab 切换，编辑模式不显示；
2、批量模式下：IP 字段变为多行 textarea（支持逗号/分号/空格/换行/斜杠/竖线分隔，自动去重），主机名变为可选前缀（留空 → 主机名 = IP；填 web → web-192.168.1.11）；
3、端口/用户名/密码/私钥/系统/应用/机房/环境/负责人/描述/标签等字段全部共享；
4、隐藏依赖单台 IP 的功能：测试连接、收集系统信息、持续添加、执行环境区块；
5、实时识别统计：有效 N · 批内重复 M（自动去重）· 已存在 K（提交时自动跳过）· 无效项列举；
6、提交：客户端预过滤已存在 IP，主进程 host-batch-import（skipExisting: true + 批内 seenKeys 查重）兑底；全部成功 → 关闭弹窗 + 成功通知；存在失败项 → 保留弹窗展示结果摘要（成功绿/跳过黄/失败红）便于修正重试；
7、Excel 批量导入行为不变（skipExisting 默认 false，仍走“存在则更新”逻辑）。

- 修改文件：`src/main/main.js`（host-batch-import 新增 skipExisting 参数 + 批内查重）、`src/renderer/js/services/hostService.js`（新增 batchAddHosts）、`src/renderer/js/components.js`（HostForm 单台/批量模式切换、parseBatchIps、saveHostBatch、renderBatchResult）、`src/renderer/css/components.css`（entry-mode-tabs / batch-ip-textarea / batch-ip-stats / batch-result 样式）
- 不做：IP 段展开（192.168.1.1-10 / CIDR，留二期）；不改 HostModel.create 单台逻辑。

## 20260917 - 脚本执行页选择面板分组折叠与主机选择优化

需求：
1、左侧脚本选择面板的「最近使用 / 未分类 / 各目录」分组头支持点击折叠/展开；
2、新增「最近添加」置顶分组（按创建时间倒序取前5）；
3、增高窗体高度，看到更多内容；
4、选择目标主机后已选主机标签白字浅底看不清；选中主机后二次修改主机的交互不明确。

**实施方案（已确认：二次修改采用「整体重新选择」模式，折叠状态 localStorage 持久化）：**
1、分组折叠：分组头变为可点击（箭头 90° 旋转动效 + hover 反馈），折叠状态存 `localStorage.te_script_group_collapsed`；搜索/标签过滤时强制展开并禁止折叠，避免命中结果被隐藏；键盘导航（↑↓）自动跳过已折叠分组内的行；
2、最近添加组：`__added__` 分组，按 created_at 倒序取前5，与「最近使用」一样仅无过滤时展示，图标用 plus；
3、高度：脚本列表 max-height 240px → 380px；主机选择弹窗表格区 max-height 400px → 520px；已选主机 chips 区 max-height 100px → 120px；
4、白字根因：`.badge` 默认样式为深色主题遗留的白字（--text-inverse）+ 浅灰底（--surface-active），改默认 color 为 --text-body，一处修复脚本执行/命令执行/文件上传/工程任务四页的所有白字 badge；已选主机 chip 改用新类 .te-host-chip（白底深字 + 描边 + IP 等宽字体）；
5、二次修改：已选主机后按钮文案变为「编辑已选主机」并显示提示行；重新打开弹窗预勾选已有主机（原有行为），确认时以勾选结果整体替换；
6、顺带修复：tokens.css 补齐缺失的 `--surface-hover` token（此前脚本行 hover 背景一直未生效）。

- 修改文件：`src/renderer/js/taskExecution.js`（折叠状态管理、__added__ 分组、分组 DOM 重构、编辑入口）、`src/renderer/index.html`（teHostEditHint 提示行）、`src/renderer/css/components.css`（.badge 色值、.te-script-list 高度、折叠样式、.te-host-chip）、`src/renderer/css/tokens.css`（新增 --surface-hover）
- 验证：node --check 语法通过。

**状态：✅ 已完成（2026-09-17）**


## 20260917 - 已选主机改为表格列表展示

需求：
右侧「选择目标主机」的已选主机展示从标签堆叠改为列表方式，每行支持单独删除 X，列表展示 IP、应用名称、负责人。
后续追加：命令执行页选择主机区域按相同逻辑处理。

**实施方案：**
1、已选主机区改为紧凑表格（.te-host-table）：列为 IP地址 / 应用名称 / 负责人 / 操作（X），IP 用等宽字体，应用名称 hover 提示所属系统；
2、每行 X 按钮单独移除该主机（事件委托绑定在列表容器，避免重复绑定），移除后自动刷新计数与「编辑已选主机」按钮状态；
3、表头 sticky 吸顶，容器 max-height 220px 超出滚动；覆盖全局 .table 最后一列 280px 宽度规则；
4、X 按钮 hover 变红色警示态；「编辑已选主机」整体替换入口保留不变；
5、删除 .te-host-chip 系列样式，改为 .te-host-table / .te-host-remove-btn；
6、命令执行页（commandExecution.js）同步实施：表格列表 + 单独删除 + 编辑双态按钮（teCommandHostEditHint）+ 弹窗表格区高度 400px→520px，复用同一套 .te-host-table 样式。

- 修改文件：`src/renderer/js/taskExecution.js`（updateSelectedHostsList 表格渲染 + handleSelectedHostsListClick 单独移除 + bindEvents 委托绑定）、`src/renderer/js/commandExecution.js`（同步逻辑）、`src/renderer/index.html`（两页 teHostEditHint / teCommandHostEditHint 提示行）、`src/renderer/css/components.css`（.te-host-table 系列，删除 .te-host-chip）
- 验证：node --check 语法通过。

**状态：✅ 已完成（2026-09-17）**

